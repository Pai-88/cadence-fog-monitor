# Parkinson's closed-loop gait garment — Python dependencies (laptop / Colab).
#
# Pinned to lower bounds only (>=), so a fresh `pip install -r requirements.txt`
# resolves a compatible recent set. The torch-free core (the `fog` package minus
# fog.model) needs only numpy + scipy; everything below that line is for a
# specific deployment or for development.
#
#   Live inference on the laptop:   numpy scipy torch fastapi uvicorn (+pyserial only
#                               for the legacy --transport serial USB tether)
#   Offline training (Colab):   + scikit-learn pandas seaborn optuna
#   Development / CI:           + pytest ruff mypy
#
# The Circuit Playground Express firmware (firmware/*.ino) is standalone C++ and
# needs none of this — only the Arduino IDE + Adafruit_CircuitPlayground.

# ── core DSP + tensors (needed by every deployment) ─────────────────────────
numpy>=1.24
scipy>=1.10

# ── live inference: trained CNN + serial link + dashboard ───────────────────
torch>=2.0
pyserial>=3.5
fastapi>=0.110
uvicorn[standard]>=0.27

# ── offline training + analysis (Colab) ─────────────────────────────────────
scikit-learn>=1.3
pandas>=2.0
seaborn>=0.13
optuna>=3.5

# ── development: tests, lint, type-check ────────────────────────────────────
pytest>=8.0
ruff>=0.4
mypy>=1.9
